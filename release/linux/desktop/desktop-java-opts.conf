# One JVM option per line.
# Example:
# -Xmx1G
# -Duser.language=zh
# On Java 25+ the launcher automatically maintains an AOT cache (JEP 514) to
# speed up startup. Set the HUSI_DISABLE_AOT_CACHE environment variable to turn
# that off, or configure your own -XX:AOT* options here to take full control.
# Fix DPI scaling.
-Dcompose.application.configure.swing.globals=true
